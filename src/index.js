import React from 'react';
// importing objects(React is the object) from react module
import ReactDOM from 'react-dom';

const element = <h1>Hello Mirriam</h1>;
ReactDOM.render(element, document.getElementById('root'));
